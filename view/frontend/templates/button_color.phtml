<?php if ($color = $block->getColor()): ?>
    <style>

        .action.primary,
        button.primary,
        .action.tocart.primary {
            background-color: <?= $block->escapeHtml($color) ?> !important;
            border-color: <?= $block->escapeHtml($color) ?> !important;
        }
        
    </style>
<?php endif; ?>